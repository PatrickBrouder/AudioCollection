extends layout

block content
  h1 Welcome to you playlists
  for playList in playlists
    div
      p= playList.name
      a(href='/listen/#{playlist.playId}') Listen
    //a(href='/listen/#{playlist.playId}') Listen
  a(href='/createNewPlaylist')
     button(type="button") Create new playlist
  
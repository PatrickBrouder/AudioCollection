extends layout

block content
  h1 Welcome to you playlists
  for playList in playlists
    div
      - var playlistId =playList.playId
      p= playList.name 
      p= foo
      a(href='/listen/#{playlistId}') Listen
  a(href='/createNewPlaylist')
     button(type="button") Create new playlist
  